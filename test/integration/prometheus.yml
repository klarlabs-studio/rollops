# Minimal Prometheus config for the integration test: scrape Prometheus itself
# every second so `up` and other self-metrics are available quickly.
global:
  scrape_interval: 1s
  evaluation_interval: 1s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
