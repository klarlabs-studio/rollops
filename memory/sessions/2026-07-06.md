---
date: 2026-07-06
---
Fleet-upgrade session — mcp bump + a small auth migration.

## Decisions Made
- **Bumped `go.klarlabs.de/mcp` v1.15.0 → v1.21.0** (#36). Crossed the v1.19.0 in-library-auth removal, which broke one call site: `internal/notify/briefkasten.go` used the removed `mcpclient.WithBearerToken`. Migrated to the caller-injected model: `mcpclient.WithHTTPHeader("Authorization", "Bearer "+token)` (same `go.klarlabs.de/mcp/client` package, an `HTTPTransportOption`). One-line swap, behavior-preserving. build + tests green.
- `go get` pulled transitive bumps (fortify v1.6.0→v1.8.1, grpc, x/net); go directive unchanged (1.26.2).

## Tasks
Done: mcp v1.21.0 bump + WithBearerToken→WithHTTPHeader migration (#36). Merged.
Remaining: nothing.
