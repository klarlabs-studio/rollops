---
date: 2026-08-14
---

## Decisions Made

- **Make it real is the near-roadmap.** Competitive review of v0.30.0: the
  engine is real and dogfooded; several advertised capabilities are unwired
  or mislabeled. RFC `docs/design/make-it-real.md`. Category is mixed-infra
  GitOps with agents as peer operators — not Argo-but-smaller.
- **Four Roady features, in order:** `real-trust` → `real-agent` →
  `real-canary` → `real-gitops`. Honesty docs are last in A; B must not
  start before that.
- **Out of scope (constraint `make-it-real-out-of-scope`):** Jsonnet,
  app-of-apps, ApplicationSet matrix, Helm install, host agent, Postgres/
  mnemos, studio billing, Istio/NGINX/SMI, replica-weighted canary,
  multi-stage approvals.
- **RolloutSet list generator approved** (multi-cluster RFC Phase 1).
- **#98 closes on an invariant:** `current` iff scanner identity equals Git
  pin. Do not block on reproducing the 18h stall.
- **Traffic fail-closed; flags stay best-effort.**

## What Shipped

Make it real Phases A–D, Roady 142/142.

Phase D (this close-out): GitHub HMAC webhook (`POST /v1/hooks/github`),
`RolloutSet` list expansion at load, Kubernetes `ignoreDifferences`, reconcile
skips until `dependsOn` is promoted. Poll remains the safety net.

Phase C: tick-driven `Stepper`; pause/resume/abort on CLI/HTTP/gRPC/MCP/UI;
honest bake/cutover names.

Phase A/B as previously recorded (freeze, boot, risk, Vault, analysis, agent-deploy, MCP observe, runbook).

## Next Session Should

Stop adding Argo checkboxes. TDD §17 leftovers need a new Roady plan before
code: schema versioning, plugin protocol, analysis P2, multi-instance, UI
act-vs-observe, RBAC taxonomy.

Always `warden run pre-push` before `git push` — Cursor overrides hooksPath,
so chore-only commits otherwise leave unattributable provenance.

