---
date: 2026-08-14
---

## Decisions Made

- **Make it real is the near-roadmap.** Competitive review of v0.30.0: the
  engine is real and dogfooded; several advertised capabilities are unwired
  or mislabeled. RFC `docs/design/make-it-real.md`. Category is mixed-infra
  GitOps with agents as peer operators — not Argo-but-smaller.
- **Four Roady features, in order:** `real-trust` → `real-agent` →
  `real-canary` → `real-gitops`. Honesty docs are last in A; B must not
  start before that.
- **Out of scope (constraint `make-it-real-out-of-scope`):** Jsonnet,
  app-of-apps, ApplicationSet matrix, Helm install, host agent, Postgres/
  mnemos, studio billing, Istio/NGINX/SMI, replica-weighted canary,
  multi-stage approvals.
- **RolloutSet list generator approved** (multi-cluster RFC Phase 1).
- **#98 closes on an invariant:** `current` iff scanner identity equals Git
  pin. Do not block on reproducing the 18h stall.
- **Traffic fail-closed; flags stay best-effort.**

## What Shipped

Phase C1 (this session): tick-driven canary `Stepper`. Apply deploys once,
persists snapshot, returns `deploying` for a positive pause. `Engine.Tick`
and reconcile advance due steps; restart restores the snapshot. Zero-pause
plans still drain inside Apply (statekit `After(0)` is not delayed — the
engine sends `NEXT`). Lease is re-acquired per tick.

Planning plus Phase A first slice (Roady verified):

- `docs/design/make-it-real.md` + Roady features `real-trust` / `real-agent` /
  `real-canary` / `real-gitops`
- `task-real-persist-freeze` — `f0d383a` Store `runtime_freeze`, persist-then-engage
- `task-real-extract-engine-boot` — `f7166ca` `internal/boot` for CLI + daemon
- `task-real-persist-risk-score` — `bb16ef6` Apply writes `RiskScore`; UI blast-radius
- `task-real-wire-vault` — `2d30a08` `secrets.FromEnv` Chain{Vault, Env}
- `task-real-wire-metric-analysis` — `11caff0` `ROLLOPS_ANALYSIS=1` opts in
- `task-real-risk-inputs-from-callers` — `68d1e55` RiskFromConfig on CLI/HTTP/gRPC/MCP/reconcile
- `task-real-traffic-fail-closed` — `32f134c` SetWeight error aborts the step
- `task-real-imageauto-current-invariant` — `17f21f5` current iff scanner==Git pin
- `task-real-cli-policy-parity` — `ec28ef2` one-shot freeze/secrets/audit/artifact tests
- `task-real-grpc-tls-client` — `a33573b` Dial uses servertls when `ROLLOPS_TLS_*` is set
- `task-real-honesty-docs` — `8b77e8e` README/TDD/wiki/vision/store/MCP match the daemon
- `task-real-deploy-agent-rbac` — `7a29d06` unbound `agent-deploy` role; `agent:*` stays plan+status
- `task-real-mcp-observe-tools` — `2fff172` MCP `list` / `history` / `drift` (status authz)
- `task-real-mcp-runbook` — `b45f503` `docs/agent-operator.md`

## Next Session Should

1. `task-real-pause-resume-abort` — CLI/HTTP/gRPC/MCP (+ UI)
2. `task-real-strategy-names` — honest bake vs canary, cutover vs blue-green

Do **not** start Phase D (webhook / RolloutSet) before C.

